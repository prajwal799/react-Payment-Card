const ComapanyLogo = ({ src }) => {
  return (
    <img
      src={src}
      style={{
        width: "100px",
        height: "100px",
        marginLeft: "34%",
        marginTop: "30px"
      }}
    />
  );
};

export default ComapanyLogo;
